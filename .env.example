# D365 Finance & Operations MCP Server Configuration

# Azure AD tenant ID (GUID or domain name)
D365_TENANT_ID=your-tenant-id

# Azure AD application (client) ID
D365_CLIENT_ID=your-client-id

# Azure AD application secret
D365_CLIENT_SECRET=your-client-secret

# D365 environment URL (without trailing slash)
# Example: https://yourorg.operations.dynamics.com
D365_ENVIRONMENT_URL=https://yourorg.operations.dynamics.com

# MCP Transport mode: "stdio" (default) or "http"
MCP_TRANSPORT=stdio

# HTTP server port (only used when MCP_TRANSPORT=http)
MCP_HTTP_PORT=3000
